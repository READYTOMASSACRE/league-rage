{
  "devDependencies": {
    "@types/colors": "^1.2.1",
    "@types/node": "^18.14.0",
    "@types/ragemp-s": "github:CocaColaBear/types-ragemp-s#master"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "date-fns": "^2.29.3",
    "reflect-metadata": "^0.1.13"
  },
  "scripts": {
    "watch": "tsc -p tsconfig.local.json --watch",
    "build": "tsc -p tsconfig.local.json",
    "build:external": "rm -rf ../../packages/league-tdm && tsc",
    "postbuild:external": "cp -R node_modules ../../packages/league-tdm"
  }
}
