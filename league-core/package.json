{
  "name": "league-core",
  "description": "League common core package",
  "version": "0.1.1",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "date-fns": "^2.29.3",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/ragemp-c": "github:CocaColaBear/types-ragemp-c",
    "@types/ragemp-s": "github:CocaColaBear/types-ragemp-s"
  },
  "scripts": {
    "build": "tsc -p tsconfig.local.json",
    "watch": "tsc -p tsconfig.local.json --watch",
    "prebuild:external": "rm -rf ../../packages/league-core",
    "build:external": "tsc -p tsconfig.local.json && tsc",
    "postbuild:external": "cp -R node_modules ../../packages/league-core",
    "watch:external": "tsc --watch",
    "source:external": "tsc -p tsconfig.local.json && tsc"
  }
}
